{
  "name": "rag-app",
  "version": "1.0.0",
  "description": "RAG application with frontend and backend",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && python main.py",
    "dev": "concurrently \"cd backend && python main.py\" \"cd frontend && npm run dev\"",
    "install-deps": "cd frontend && npm install && cd backend && pip install -r requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}