[phases.setup]
nixPkgs = ["nodejs", "python3"]

[phases.install]
cmds = [
    "npm install",
    "cd frontend && npm install",
    "cd backend && python3 -m pip install -r requirements.txt"
]

[phases.build]
cmds = [
    "npm run build"
]

[start]
cmd = "npm run start" 